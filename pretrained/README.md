# Pretrained assets

This directory is reserved for externally obtained model weights. Record the
source, version or commit, checksum, and intended use next to each local asset.

Weights trained by this SeMamba++ project belong in `checkpoints/`. Runtime
weight files are ignored by Git.
